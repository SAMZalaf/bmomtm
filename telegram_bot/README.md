# بوت تيليغرام لبيع البروكسيات

مشروع بوت تيليغرام متكامل لإدارة وبيع الخدمات والبروكسيات مع لوحة تحكم ويب حديثة.

## المحتويات

### ملفات البوت الأساسية
- **bot.py** - البوت الرئيسي (Telegram Bot Handler)
- **config.py** - الإعدادات والثوابت والرموز
- **run.py** - ملف التشغيل الرئيسي

### منطق البوت
- **bot_admin.py** - معالج أوامر الآدمن
- **bot_customer.py** - معالج تفاعلات العميل
- **bot_keyboards.py** - إنشاء الكيبوردات والأزرار
- **bot_utils.py** - دوال مساعدة عامة

### إدارة الأزرار الديناميكية
- **dynamic_buttons.py** - مدير الأزرار (CRUD operations)
- **dynamic_buttons_handler.py** - معالج أحداث الأزرار
- **dynamic_buttons_handler_*.py** - معالجات متخصصة

### الخدمات والتكاملات
- **non_voip_unified.py** - مزود البروكسيات الموحد

### واجهات إضافية
- **miniapp.html** - تطبيق Mini App
- **miniapp_server.py** - خادم Mini App

### قاعدة البيانات
- **proxy_bot.db** - SQLite Database (مشتركة مع لوحة التحكم)

### المتطلبات
- **requirements.txt** - مكتبات Python المطلوبة

---

## البنية

```
telegram_bot/
├── bot.py                          # البوت الرئيسي
├── config.py                       # الإعدادات
├── run.py                          # نقطة الدخول
├── *.py                            # باقي الملفات
├── proxy_bot.db                    # قاعدة البيانات
├── requirements.txt                # المكتبات
├── web_ui/                         # لوحة التحكم
│   ├── client/                     # React Frontend
│   ├── server/                     # Express Backend
│   └── ...
└── README.md                       # هذا الملف
```

---

## التثبيت

```bash
# 1. الذهاب للمجلد
cd telegram_bot

# 2. تثبيت المكتبات
pip install -r requirements.txt

# 3. إضافة رمز البوت
# عدّل config.py وأضف: BOT_TOKEN = "your_token_here"

# 4. تشغيل البوت
python run.py
```

---

## الميزات

✅ **الأزرار الديناميكية**
- إضافة/تعديل/حذف أزرار من لوحة التحكم
- دعم الأزرار الهرمية (Parent/Child)
- تفعيل/تعطيل دون حذف

✅ **لوحة التحكم الويب**
- واجهة عربية RTL حديثة
- معاينة فورية للأزرار
- تصدير/استيراد البيانات
- موجودة في `web_ui/`

✅ **تتبع الطلبات**
- سجل جميع الطلبات والمبيعات
- إحصائيات مباشرة
- تقارير قابلة للتصدير

✅ **دعم اللغات**
- عربي ✅
- إنجليزي ✅

---

## الاستخدام

### تشغيل البوت

```bash
python run.py
```

### تشغيل لوحة التحكم

```bash
cd web_ui
npm run dev
```

### تشغيل كل شيء

```bash
# في terminal منفصل
python run.py        # البوت

# في terminal آخر
cd web_ui
npm run dev          # لوحة التحكم
```

---

## ملاحظات مهمة

⚠️ **قاعدة البيانات مشتركة:**
- التعديلات في لوحة التحكم تؤثر على البوت فوراً
- ملفات WAL (.shm و .wal) مهمة - لا تحذفها

🔒 **الأمان:**
- احفظ نسخة احتياطية قبل تعديلات كبيرة
- استخدم لوحة التحكم بدلاً من تعديل الملفات مباشرة
- لا تشارك رمز البوت (token)

---

## المتطلبات

- Python 3.7+
- Node.js 16+ (لواجهة الويب)
- رمز بوت تيليغرام (من @BotFather)

---

## الترخيص

محمود ظلاف - 2025

---

لمزيد من التفاصيل، انظر `SETUP_INSTRUCTIONS.md`
