# SMSPool Purchase Flow - نظام الشراء المتكامل

## نظرة عامة
تم تطوير نظام شراء كامل ومتكامل لخدمة SMS Bull (SMSPool) يطابق تماماً نظام Noon VOIP الناجح.

## التدفق الكامل للشراء

### 1. نقطة البداية - زر الشراء
```
المستخدم ← زر "🛒 شراء رقم" (sp_buy)
       ↓
handle_buy_sms() ← يفتح Inline Query للبحث
```

**الوظيفة**: `handle_buy_sms()`
- التحقق من تفعيل الخدمة
- التحقق من وجود API key
- عرض رسالة توجيهية للبحث
- زر "🔍 ابحث عن خدمة أو دولة" → يفتح Inline Query

---

### 2. البحث عن الدول والخدمات - Inline Query
```
المستخدم ← يكتب في Inline Query
       ↓
handle_smspool_inline_query() ← معالج البحث
       ↓
عرض النتائج (دول أو خدمات)
```

**الوظيفة**: `handle_smspool_inline_query()`

#### أ. عند البحث الفارغ:
- عرض الدول الشائعة أولاً: US, GB, CA, DE, FR, NL, RU, IN, PH, ID
- كل دولة تعرض مع علمها 🇺🇸 🇬🇧 🇨🇦
- يصل إلى 20 دولة في النتائج

#### ب. عند البحث عن نص:
- البحث في أسماء الدول (United States, United Kingdom, etc.)
- البحث في أكواد الدول (US, GB, CA)
- البحث في أسماء الخدمات (WhatsApp, Google, Telegram)
- عرض النتائج المطابقة (حتى 10 نتائج لكل فئة)

---

### 3. اختيار الدولة وعرض الخدمات
```
المستخدم ← ينقر على دولة من Inline Query
       ↓
sp_country_{country_id} ← callback
       ↓
عرض قائمة الخدمات المتاحة للدولة
```

**معالج Callback**: `sp_country_{country_id}`
- جلب جميع الخدمات المتاحة من API
- الحصول على الأسعار لكل خدمة (مع الدولة المختارة)
- عرض حتى 20 خدمة مع:
  - أيقونة الخدمة: 💚 WhatsApp, ✈️ Telegram, 🔍 Google, 📘 Facebook, 📷 Instagram
  - اسم الخدمة
  - السعر بالكريديت (بعد تطبيق نسبة الربح)

---

### 4. تأكيد الشراء
```
المستخدم ← ينقر على خدمة
       ↓
sp_buy_{country_id}_{service_id} ← callback
       ↓
confirm_purchase() ← عرض تفاصيل الشراء
```

**الوظيفة**: `confirm_purchase()`
- جلب معلومات الخدمة والدولة
- حساب السعر الفعلي من API
- تطبيق نسبة الربح
- عرض:
  - أيقونة وأسم الخدمة: 💚 WhatsApp
  - علم واسم الدولة: 🇺🇸 United States
  - السعر بالكريديت
  - رصيد المستخدم الحالي
  - الرصيد بعد الشراء
- زر "✅ تأكيد الشراء"
- زر "❌ إلغاء"

---

### 5. معالجة الشراء وإتمامه
```
المستخدم ← ينقر "تأكيد الشراء"
       ↓
sp_confirm_{country_id}_{service_id} ← callback
       ↓
process_purchase() ← معالجة الشراء
       ↓
✅ تم الشراء بنجاح
```

**الوظيفة**: `process_purchase()`

#### خطوات المعالجة:
1. **التحقق من الرصيد**: التأكد من كفاية الرصيد
2. **عرض رسالة معالجة**: "⏳ جاري معالجة الطلب..."
3. **استدعاء API للشراء**: `api.purchase_sms(country_id, service_id)`
4. **عند النجاح**:
   - خصم الرصيد من المستخدم
   - حفظ الطلب في قاعدة البيانات `smspool_orders`
   - عرض رسالة النجاح مع تفاصيل الرقم
   - بدء المراقبة التلقائية للرسائل
5. **عند الفشل**:
   - عرض رسالة خطأ واضحة
   - تسجيل الخطأ في اللوجات

#### رسالة النجاح تحتوي على:
```
✅ تم شراء الرقم بنجاح!

📱 الرقم: +1234567890
🌍 الدولة: United States
📱 الخدمة: WhatsApp
⏱️ صالح لمدة: 10 دقيقة

💡 انتظر الرسالة وستظهر تلقائياً
```

#### الأزرار المتاحة:
- 🔄 فحص الرسالة (يدوي)
- 📤 إعادة إرسال
- ❌ إلغاء واسترداد الرصيد
- 🔙 رجوع

---

### 6. المراقبة التلقائية للرسائل
```
بعد الشراء ← بدء مراقبة تلقائية كل 10 ثوانٍ
       ↓
check_sms_job() ← فحص وصول الرسالة
       ↓
عند وصول الرسالة ← إرسال إشعار للمستخدم
```

**الوظيفة**: `check_sms_job()`
- تعمل تلقائياً كل 10 ثوانٍ
- تفحص API لمعرفة حالة الرسالة
- عند وصول الرسالة:
  - تحديث حالة الطلب في قاعدة البيانات
  - إرسال رسالة للمستخدم بالكود
  - إيقاف المراقبة التلقائية
- عند الإلغاء أو الانتهاء:
  - تحديث الحالة وإيقاف المراقبة

---

## الوظائف الإضافية

### فحص الرسالة يدوياً
**Callback**: `sp_check_{order_id}`
**الوظيفة**: `check_order_status()`
- فحص حالة الطلب من API
- عرض الحالة الحالية:
  - ⏳ في انتظار الرسالة
  - ✅ تم استلام الرسالة (مع الكود)
  - ❌ تم إلغاء الطلب
  - ⏰ انتهت الصلاحية

### إلغاء الطلب واسترداد الرصيد
**Callback**: `sp_cancel_{order_id}`
**الوظيفة**: `cancel_order()`
1. التحقق من ملكية المستخدم للطلب
2. استدعاء API لإلغاء الرقم
3. إعادة الرصيد للمستخدم
4. تحديث حالة الطلب في قاعدة البيانات
5. إيقاف المراقبة التلقائية

### إعادة إرسال الرسالة
**Callback**: `sp_resend_{order_id}`
**الوظيفة**: `resend_sms()`
- طلب إعادة إرسال الرسالة من API
- عرض رسالة تأكيد

### عرض طلبات المستخدم
**Callback**: `sp_my_orders`
**الوظيفة**: `show_user_orders()`
- عرض آخر 10 طلبات للمستخدم
- عرض الحالة مع أيقونات: ⏳ ✅ ❌ ⏰
- زر لفحص كل طلب نشط

---

## قاعدة البيانات

### جدول `smspool_orders`
يحفظ جميع الطلبات مع التفاصيل:
```sql
- id: رقم الطلب التسلسلي
- user_id: معرف المستخدم
- order_id: معرف الطلب من SMSPool API
- number: رقم الهاتف
- country: اسم الدولة
- country_id: معرف الدولة
- service: اسم الخدمة
- service_id: معرف الخدمة
- pool: المجموعة
- cost_price: سعر التكلفة
- sale_price: سعر البيع (بعد نسبة الربح)
- status: الحالة (pending, received, cancelled, expired)
- sms_code: كود الرسالة
- full_sms: نص الرسالة الكامل
- expires_at: تاريخ انتهاء الصلاحية
- created_at: تاريخ الإنشاء
- updated_at: تاريخ آخر تحديث
```

### جدول `smspool_settings`
إعدادات الخدمة:
```sql
- id: 1 (دائماً)
- api_key: مفتاح API من SMSPool
- enabled: تفعيل/تعطيل الخدمة
- margin_percent: نسبة الربح (افتراضي 30%)
- created_at: تاريخ الإنشاء
- updated_at: تاريخ آخر تحديث
```

---

## معالجة الأخطاء

### أكواد الأخطاء المشفرة
لإخفاء تفاصيل API عن المستخدمين:
```python
ERROR_CODES = {
    '0x0000': 'رصيد الحساب غير كافٍ',
    '0x0001': 'الخدمة غير متوفرة حالياً',
    '0x0002': 'خطأ في الاتصال بالموقع',
    '0x0003': 'تم رفض الطلب',
    '0x0004': 'انتهت مهلة الاتصال',
    '0x0005': 'مفتاح API غير صحيح',
    '0x0006': 'تم تجاوز حد الطلبات',
    '0x0007': 'الطلب غير موجود',
    '0x0008': 'فشل في جلب الرسالة',
    '0x0009': 'خطأ غير متوقع',
    '0x000A': 'الخدمة غير متاحة'
}
```

### دالة تحديد كود الخطأ
`get_error_code_from_message(error_message)` - تحلل رسالة الخطأ وتعيد الكود المناسب

---

## لوحة التحكم (Admin)

### قائمة الآدمن
**Callback**: `sp_admin_menu`
**الوظيفة**: `smspool_admin_menu()`

#### الوظائف المتاحة:
1. **تعيين مفتاح API**: `sp_admin_set_key`
   - طلب إدخال مفتاح API من SMSPool
   - التحقق من صحة المفتاح بجلب الرصيد
   - حفظ المفتاح في قاعدة البيانات

2. **تفعيل/تعطيل الخدمة**: `sp_admin_toggle`
   - تبديل حالة الخدمة (مفعّلة/معطّلة)

3. **تعديل نسبة الربح**: `sp_admin_margin`
   - طلب إدخال نسبة الربح الجديدة (0-500%)
   - حفظ النسبة في قاعدة البيانات

4. **عرض معلومات الخدمة**:
   - حالة مفتاح API (مُعيّن/غير مُعيّن)
   - رصيد الحساب في SMSPool
   - حالة التفعيل
   - نسبة الربح الحالية
   - عدد الطلبات النشطة

---

## مقارنة مع NonVoip

| الميزة | NonVoip | SMSPool |
|--------|---------|---------|
| نقطة البداية | اختيار نوع الرقم أولاً | البحث المباشر في الدول والخدمات |
| البحث | Inline Query بنوع الرقم | Inline Query مباشر |
| عرض النتائج | منتجات مع صور | دول وخدمات مع أعلام وأيقونات |
| اختيار الدولة | أمريكا فقط (مع ولايات) | جميع الدول المتاحة |
| عرض الخدمات | من cache محلي | من API مباشرة |
| تأكيد الشراء | ✅ | ✅ |
| معالجة الشراء | ✅ | ✅ |
| المراقبة التلقائية | ✅ | ✅ |
| إلغاء واسترداد | ✅ | ✅ |

---

## الملفات المعدلة

### `smspool_service.py`
الملف الرئيسي - يحتوي على جميع الوظائف:
- `handle_buy_sms()` - نقطة البداية
- `handle_smspool_inline_query()` - معالج البحث
- `confirm_purchase()` - تأكيد الشراء
- `process_purchase()` - معالجة الشراء
- `check_sms_job()` - المراقبة التلقائية
- `cancel_order()` - الإلغاء
- `check_order_status()` - فحص الحالة
- `resend_sms()` - إعادة الإرسال
- `show_user_orders()` - عرض الطلبات
- `smspool_admin_menu()` - لوحة الآدمن

---

## التكامل مع Bot.py

### المطلوب إضافته في bot.py:

```python
# Import
from smspool_service import (
    handle_smspool_callback,
    handle_smspool_inline_query,
    smspool_admin_menu,
    handle_smspool_admin_callback,
    handle_admin_api_key_input,
    handle_admin_margin_input
)

# Inline Query Handler
application.add_handler(InlineQueryHandler(handle_smspool_inline_query))

# Callback Handler
application.add_handler(CallbackQueryHandler(
    handle_smspool_callback,
    pattern='^sp_'
))

# Admin Callback Handler
application.add_handler(CallbackQueryHandler(
    handle_smspool_admin_callback,
    pattern='^sp_admin_'
))
```

---

## الاختبار

### خطوات الاختبار:
1. ✅ تعيين API key من لوحة الآدمن
2. ✅ تفعيل الخدمة
3. ✅ اختيار زر "شراء رقم"
4. ✅ البحث عن دولة (مثل: United States)
5. ✅ اختيار دولة من النتائج
6. ✅ اختيار خدمة (مثل: WhatsApp)
7. ✅ تأكيد الشراء
8. ✅ التحقق من خصم الرصيد
9. ✅ التحقق من حفظ الطلب
10. ✅ انتظار الرسالة التلقائية
11. ✅ فحص الرسالة يدوياً
12. ✅ إلغاء الطلب واسترداد الرصيد

---

## معايير النجاح ✅

- ✅ جميع وظائف الشراء تعمل بشكل كامل
- ✅ عرض قائمة الدول والخدمات صحيح
- ✅ الشراء يتم بنجاح والطلب يتم حفظه
- ✅ رسائل الخطأ واضحة ومفيدة (عربي/إنجليزي)
- ✅ الرصيد يتم خصمه بشكل صحيح
- ✅ المراقبة التلقائية تعمل
- ✅ الإلغاء واسترداد الرصيد يعمل
- ✅ لوحة التحكم للآدمن كاملة
- ✅ التدفق يطابق NonVoip الناجح

---

## المستقبل - تحسينات محتملة

1. **Cache للمنتجات**: حفظ الدول والخدمات محلياً لتسريع البحث
2. **صور الخدمات**: إضافة صور/أيقونات أكبر في Inline Query
3. **تصفية متقدمة**: تصفية حسب السعر أو التوفر
4. **إحصائيات**: تتبع المبيعات والأرباح
5. **إشعارات**: إشعار عند وصول الرسالة (push notification)
6. **تاريخ الطلبات**: عرض جميع الطلبات السابقة مع التفاصيل
7. **تصدير البيانات**: تصدير الطلبات إلى CSV/Excel

---

## الخلاصة

تم تطوير نظام شراء متكامل لـ SMS Bull (SMSPool) يطابق تماماً نظام Noon VOIP الناجح:

✅ **التدفق الكامل**: من البحث حتى استلام الرسالة
✅ **Inline Query**: بحث سريع عن الدول والخدمات
✅ **معالجة الشراء**: خصم الرصيد وحفظ الطلبات
✅ **المراقبة التلقائية**: فحص الرسائل كل 10 ثوانٍ
✅ **الإلغاء واسترداد**: إمكانية استرداد الرصيد
✅ **لوحة الآدمن**: إدارة كاملة للخدمة
✅ **معالجة الأخطاء**: رسائل واضحة ومفيدة
✅ **دعم اللغتين**: عربي وإنجليزي

---

تاريخ التطوير: 2026-02-02
المطور: AI Assistant
الإصدار: 1.0.0
