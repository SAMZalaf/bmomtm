# 📱 SMSPool Integration - دليل الاستخدام

## 📖 نظرة عامة

تم دمج خدمة SMSPool بنجاح في البوت. يمكن الآن للمستخدمين شراء أرقام افتراضية للتحقق من أكثر من 1,279 خدمة في 151 دولة.

---

## ✅ الحالة الحالية

| المكون | الحالة | الملاحظات |
|--------|--------|-----------|
| مفتاح API | ✅ محفوظ | في قاعدة البيانات بأمان |
| الاتصال | ✅ يعمل | الرصيد: $0.00 |
| الخدمات | ✅ 1,279 | جاهزة للاستخدام |
| الدول | ✅ 151 | جاهزة للاستخدام |
| الزر | ✅ مضاف | في القائمة الرئيسية |
| الهامش | ✅ 30% | قابل للتعديل |

---

## 🚀 كيفية الاستخدام

### للمستخدمين:

1. **فتح البوت** `/start`
2. **اختيار** "📱 أرقام SMSPool"
3. **اختيار الدولة** (مثلاً: United States)
4. **اختيار الخدمة** (مثلاً: Google, WhatsApp, Telegram)
5. **الشراء** والحصول على الرقم
6. **جلب الرسائل** لاستلام كود التحقق

### للإدارة:

#### تغيير نسبة الربح:
```python
from smspool_service import SMSPoolDB

db = SMSPoolDB()
db.set_margin_percent(35)  # تغيير إلى 35%
```

#### تفعيل/تعطيل الخدمة:
```python
from smspool_service import SMSPoolDB

db = SMSPoolDB()
db.set_enabled(True)   # تفعيل
db.set_enabled(False)  # تعطيل
```

#### التحقق من الرصيد:
```python
from smspool_service import SMSPoolAPI, SMSPoolDB

db = SMSPoolDB()
api = SMSPoolAPI(api_key=db.get_api_key())
result = api.get_balance()
print(f"الرصيد: ${result['balance']}")
```

---

## 🔧 الإعدادات

### قاعدة البيانات: `smspool_settings`

| العمود | القيمة الحالية | الوصف |
|--------|----------------|-------|
| `api_key` | TM8g...zQNU | مفتاح API (32 حرف) |
| `enabled` | 1 | حالة التفعيل |
| `margin_percent` | 30.0 | نسبة الربح % |

### تعديل الإعدادات عبر SQL:

```sql
-- تغيير نسبة الربح
UPDATE smspool_settings 
SET margin_percent = 35 
WHERE id = 1;

-- تعطيل الخدمة
UPDATE smspool_settings 
SET enabled = 0 
WHERE id = 1;

-- التحقق من الإعدادات
SELECT * FROM smspool_settings;
```

---

## 💰 حساب الأسعار

الصيغة المستخدمة:
```
سعر البيع = سعر التكلفة × (1 + نسبة_الربح / 100)
```

أمثلة مع هامش 30%:

| سعر API | سعر للمستخدم | الربح |
|---------|--------------|-------|
| $0.50 | $0.65 | $0.15 |
| $1.00 | $1.30 | $0.30 |
| $2.00 | $2.60 | $0.60 |
| $5.00 | $6.50 | $1.50 |

---

## 📊 إحصائيات الخدمة

### الخدمات المتاحة (1,279):
- Google, WhatsApp, Telegram, Facebook
- Instagram, Twitter, TikTok, Snapchat
- Microsoft, Apple, Amazon, Netflix
- وأكثر من 1,270 خدمة أخرى

### الدول المتاحة (151):
- 🇺🇸 United States
- 🇬🇧 United Kingdom
- 🇨🇦 Canada
- 🇦🇺 Australia
- وأكثر من 145 دولة أخرى

---

## 🛠️ الأوامر المساعدة

### اختبار الاتصال:
```bash
cd /home/engine/project/telegram_bot
source venv/bin/activate
python3 test_smspool_api.py
```

### التحقق النهائي:
```bash
python3 final_verification.py
```

### مراقبة السجلات:
```bash
tail -f logs/bot.log | grep -i smspool
```

### إعادة تشغيل البوت:
```bash
./restart_bot.sh
# أو
./start_all.sh
```

---

## 🔍 استكشاف الأخطاء

### المشكلة: الخدمة لا تظهر في البوت

**الحل:**
```bash
# 1. التحقق من الزر
sqlite3 proxy_bot.db "SELECT * FROM dynamic_buttons WHERE button_key = 'smspool_main';"

# 2. التحقق من حالة التفعيل
sqlite3 proxy_bot.db "SELECT enabled FROM smspool_settings;"

# 3. إعادة تشغيل البوت
./restart_bot.sh
```

### المشكلة: خطأ في الاتصال بـ API

**الحل:**
```bash
# 1. التحقق من المفتاح
sqlite3 proxy_bot.db "SELECT substr(api_key, 1, 20) FROM smspool_settings;"

# 2. اختبار الاتصال
python3 test_smspool_api.py

# 3. التحقق من الرصيد على الموقع
# https://www.smspool.net/dashboard
```

### المشكلة: فشل الشراء

**الأسباب المحتملة:**
1. ⚠️ رصيد الحساب غير كافي ($0.00)
2. ❌ الخدمة غير متوفرة للدولة المختارة
3. ❌ الخدمة معطلة مؤقتاً من SMSPool

**الحل:**
- إعادة شحن الحساب على موقع SMSPool
- تجربة دولة أو خدمة أخرى
- التحقق من السجلات: `logs/bot.log`

---

## 📝 ملاحظات مهمة

### ⚠️ الرصيد الحالي

الرصيد حالياً: **$0.00**

لاستخدام الخدمة، يجب:
1. زيارة: https://www.smspool.net/dashboard
2. إعادة شحن الحساب
3. الانتظار حتى يتم تفعيل الرصيد

### 🔒 الأمان

- ✅ المفتاح محفوظ في قاعدة البيانات فقط
- ✅ لا يظهر المفتاح في الكود أو السجلات
- ✅ معالجة آمنة للأخطاء
- ✅ لا توجد بيانات حساسة مكشوفة

### 📈 الأداء

- ✅ التخزين المؤقت (Cache) للخدمات والدول (5 دقائق)
- ✅ وقت استجابة API: < 5 ثواني
- ✅ معالجة متعددة للطلبات
- ✅ لا يؤثر على أداء البوت

---

## 📚 الموارد

### وثائق SMSPool:
- **الموقع الرسمي**: https://www.smspool.net/
- **لوحة التحكم**: https://www.smspool.net/dashboard
- **وثائق API**: https://www.smspool.net/article/how-to-use-the-smspool-api
- **الدعم الفني**: https://www.smspool.net/contact

### الملفات المهمة:
- `smspool_service.py` - خدمة API الرئيسية
- `test_smspool_api.py` - سكريبت الاختبار
- `final_verification.py` - التحقق النهائي
- `SMSPOOL_SETUP_REPORT.md` - تقرير الإعداد
- `SMSPOOL_README.md` - هذا الملف

---

## 🎯 الخطوات التالية

### للبدء في الاستخدام:

1. ✅ **إعداد المفتاح** - مكتمل
2. ✅ **اختبار الاتصال** - مكتمل
3. ⏳ **إعادة شحن الحساب** - مطلوب
4. ⏳ **إعادة تشغيل البوت** - مطلوب
5. ⏳ **اختبار الشراء** - بعد إعادة الشحن

### للتطوير المستقبلي:

- [ ] إضافة لوحة تحكم في واجهة الويب
- [ ] إضافة تقارير إحصائية للمبيعات
- [ ] إضافة نظام إشعارات للرصيد المنخفض
- [ ] إضافة دعم للأرقام المستأجرة (Rentals)
- [ ] إضافة دعم للعروض الخاصة

---

## 📞 الدعم

للمساعدة أو الاستفسارات:
1. راجع هذا الملف أولاً
2. شغل `python3 final_verification.py` للتحقق
3. راجع السجلات في `logs/bot.log`
4. اتصل بالدعم الفني لـ SMSPool

---

**آخر تحديث**: 2026-02-02  
**الحالة**: ✅ جاهز للإنتاج (بعد إعادة الشحن)
